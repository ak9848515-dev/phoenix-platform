# services

This folder contains service-layer abstractions for external integrations and application support concerns.
Use it for API clients, storage wrappers, platform integrations, and other reusable services that sit behind the UI.
