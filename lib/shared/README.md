# shared

This folder is for reusable UI and logic components that are not tied to a single feature.
Examples include common widgets, helpers, extensions, and generic patterns that multiple parts of the app can reuse.
